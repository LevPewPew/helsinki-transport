type Marker {
  lat: Number,
  lng: Number
}

type Query {
  routeMarkers: [Marker]
}

# TODO i am sure since i am using direct writes that everything
# in here doesn't actually do anything. lol dumb tutorial....
# TODO change this stuff to use mutations/resolves, leave here for now.