type Marker {
  lat: Number,
  lng: Number
}

type Query {
  routeMarkers: [Marker]
}

type Query {
  modalDisplayed: Boolean!
}
# TODO pretty sure since i am using direct writes that everything
# in here doesn't actually do anything. lol dumb tutorial....