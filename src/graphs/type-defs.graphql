type PlaceHolderIgnore {
  lat: Number,
  lng: Number
}

type Query {
  routeStops: [PlaceHolderIgnore]
}

# TODO i am sure since i am using direct writes that everything
# in here doesn't actually do anything. i blame bad tutorial.... lolol